INFO ABOUT THE BINARIES:
The binaries are stored in the 'Binary/' folder. These binaries are built on Termux running on a 64 Bit ARM CPU, on Android 10 on an Samsung S9. If you can't run these on a device with a 64 bit ARM CPU, try using the Python versions(stored in the 'Python/' folder) instead. 

Read the license for Copyright info.
